let enames=['RG','SG','PG','NM']

//create new array based on existing array
let new_Enames=[]
//iterate existing array and push element into new array

for(let ename of enames ){
    new_Enames.push(ename)
}
console.log(enames)
console.log(new_Enames)